import React from 'react';
import '../stylesheets/Filters.css';

function Filters() {
  return (
    <form htmlFor="" className="Filters">
      <input type="text" id="" name="" className="Input" maxlength="30" placeholder="Nombre del personaje" required />
    </form>
  );
}

export default Filters;
